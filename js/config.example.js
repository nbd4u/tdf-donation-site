// config.example.js — example placeholders only. Do NOT commit real secrets.
const CONFIG = {
  RAZORPAY_KEY: "REPLACE_LATER",
  GOOGLE_SHEET_URL: "REPLACE_LATER",
  PAYPAL_CLIENT_ID: "REPLACE_LATER"
};

// Copy this file to js/config.js on the server and set real values.
